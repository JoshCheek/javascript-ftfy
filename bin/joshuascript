#!/usr/bin/env ruby -W0
$LOAD_PATH.unshift File.expand_path('../lib', __dir__)
require 'joshua_script'

program = $stdin.read
begin
  $stdin.reopen '/dev/tty'
rescue Errno::ENXIO
  # device not configured for tty
end
JoshuaScript.eval program
